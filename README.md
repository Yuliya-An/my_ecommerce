# My E-commerce Core

Проект представляет собой базовое ядро для интернет-магазина, реализованное с использованием ООП. Основная цель — создать структуру для управления товарами и категориями.

## Описание проекта
В ходе работы были созданы основные сущности магазина: классы Product и Category. Реализована логика инициализации объектов и автоматический подсчет общего количества категорий и товаров в системе.

## Реализованный функционал
- Класс Product: хранит информацию о названии, описании, цене и количестве товара.
- Класс Category: объединяет товары в группы, ведет учет общего числа категорий и продуктов.
- Загрузка данных: добавлена функция для чтения файла products.json и создания на его основе объектов классов.
- Тестирование: написаны тесты с помощью pytest для проверки инициализации и работы счетчиков.
- Проверка кода: проект проверен с помощью flake8 на соответствие PEP 8 и mypy на корректность типов.

## Стек технологий
- Python 3.10+
- Poetry (управление зависимостями)
- pytest, flake8, mypy

## Инструкция по запуску

1. Установка зависимостей:
   poetry install

2. Запуск программы:
   poetry run python src/main.py

3. Запуск тестов:
   poetry run python -m pytest

4. Проверка стиля и типов:
   - Стиль кода: `flake8 src/ --ignore=E721, E402`
   - Типы данных: `mypy --explicit-package-bases src/`

## Новый функционал

В проект добавлена система наследования классов:

### Классы наследники
- **Smartphone** — класс для представления смартфонов с атрибутами: производительность, модель, память, цвет
- **LawnGrass** — класс для представления газонной травы с атрибутами: страна производства, срок прорастания, цвет

### Сложение объектов
Реализована перегрузка оператора `+` для объектов одного типа:
- При сложении двух смартфонов или двух трав возвращается новый объект с суммированной стоимостью и количеством
- При попытке сложить объекты разных классов выбрасывается `TypeError` с использованием функции `type()`

### Запуск демонстрации
Для проверки работоспособности запустите файл `main.py`:
bash
python main.py

### Новая функциональность (Задание 16.1)
- Реализовано наследование классов: созданы классы-наследники `Smartphone` и `LawnGrass` от базового класса `Product`.
- Реализована перегрузка оператора сложения `__add__` с ограничением по типам с помощью функции `type()`.
- Добавлена защита метода `add_product` в классе `Category` с использованием `isinstance()`.
- Покрыто тестами с использованием `pytest`, общий процент покрытия функционального кода — 89%.